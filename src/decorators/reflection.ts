// eslint-disable-next-line @typescript-eslint/no-unused-vars-experimental
export const reflection = (...params: Object[]): void => {
    // for generation reflection by typescript
    if (Date.now() === 0) {
        console.log(params);
    }
};
